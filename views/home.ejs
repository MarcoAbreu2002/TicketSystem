<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/header.ejs') %>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-color: #f1f4f8; /* Light background color */
            color: #333;
        }

        /* Navigation bar styling */
        .navbar {
            background-color: #007bff;
            position: fixed;
            width: 100%;
            z-index: 1000;
            top: 0;
            padding: 15px 20px;
            box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
            color: white;
        }

        .navbar a {
            color: white;
            text-decoration: none;
            font-weight: bold;
        }

        /* Main content styling */
        .main-content {
            padding-top: 80px; /* Adjust this if the navbar height changes */
            text-align: center;
        }

/* Welcome section */
.welcome-box {
    background-color: #ffffff;
    border-radius: 8px;
    padding: 30px;
    margin: 20px auto;
    max-width: 1500px; /* Adjust this value as needed */
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
    display: flex;
    align-items: center;
    gap: 20px; /* Space between image and text */
}
@media (max-width: 768px) {
    .welcome-box {
        max-width: 90%; /* Adjust to fit smaller screens */
    }
}



        .welcome-box img {
            width: 100%; /* or a specific width like 500px */
            max-width: 300px; /* Adjust as needed */
            border-radius: 8px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            flex-shrink: 0; /* Prevents image from shrinking */
        }

        .welcome-box h1 {
            color: #007bff;
            margin-bottom: 15px;
        }

        .welcome-box p {
            font-size: 1.2em;
            color: #555;
            line-height: 1.6;
        }

        /* Buttons section */
        .button-container {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 20px;
            margin-top: 30px;
        }

        .button-box {
            background-color: #ffffff;
            border: 2px solid #007bff;
            border-radius: 8px;
            padding: 15px;
            max-width: 200px;
            text-align: center;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }

        .button-box img {
            width: 60px;
            height: 60px;
            margin-bottom: 10px;
        }

        .button-box p {
            font-size: 1.1em;
            color: #007bff;
        }

        .button-box a {
            display: block;
            color: #007bff;
            text-decoration: none;
        }

        .button-box:hover {
            background-color: #007bff;
            color: #ffffff;
        }

        .button-box:hover p {
            color: #ffffff;
        }

        .button-box:hover img {
            filter: brightness(0) invert(1);
        }

        /* Special "Ticket It" button */
        .ticket-button {
            background-color: #007bff;
            color: #ffffff;
            border: none;
            padding: 15px 30px;
            border-radius: 8px;
            text-align: center;
            text-decoration: none;
            font-size: 1.5em;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            transition: background-color 0.3s, box-shadow 0.3s;
        }

        .ticket-button:hover {
            background-color: #0056b3;
            color: #c0b7b7;
            box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.2);
        }

        /* Floating chat icon */
        .floating-chat-icon {
            position: fixed;
            bottom: 20px;
            right: 20px;
            background-color: #007bff;
            border: none;
            border-radius: 50%;
            width: 60px;
            height: 60px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            cursor: pointer;
            z-index: 1000;
        }

        .floating-chat-icon img {
            width: 30px;
            height: 30px;
            filter: brightness(0) invert(1);
        }

        .floating-chat-icon:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <%- include('./partials/navbar.ejs') %>

    <div class="main-content">
        <div class="welcome-box">
            <img src="../public/images/helpdesk.png" alt="Help Desk">
            <div>
                <h1>Welcome to Your Help Desk System</h1>
                <p>
                    Our help desk system is designed to assist you in resolving any issues or questions you might have. Here’s how you can use it:
                    <ul style="list-style: disc; text-align: left; margin: 10px auto; max-width: 600px;">
                        <li><strong>Login:</strong> Access your account to manage your support requests and communicate with our team.</li>
                        <li><strong>Create Tickets:</strong> Submit a ticket detailing your issue for our team to address.</li>
                        <li><strong>Chat with Agents:</strong> Use our real-time chat feature to get immediate assistance from our support team.</li>
                    </ul>
                    If you don’t have an account yet, you can create one by clicking the "Ticket It" button below.
                </p>
                <a href="/create" class="ticket-button">
                    Ticket It
                </a>
            </div>
        </div>
    </div>

    <!-- Floating chat icon -->
    <div class="floating-chat-icon" onclick="window.location.href='/chat';">
        <img src="./public/images/chat.png" alt="Chat">
    </div>

    <%- include('./partials/scripts.ejs') %>
</body>
</html>
